apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-portal
  namespace: dev
  labels:
    app: api-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-portal
  template:
    metadata:
      labels:
        app: api-portal
    spec:
      containers:
        - name: api-portal
          envFrom:
            - configMapRef:
                name: api-portal-config
          image: nishlashakya/api-portal:0.0.4
          imagePullPolicy: Always
          ports:
            - containerPort: 80
